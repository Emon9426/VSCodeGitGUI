{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "运行 CommitMap 扩展",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "npm: watch",
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
